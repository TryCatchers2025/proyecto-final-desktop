<UserControl x:Class="PereMaria.GestorHotel.Views.RoomsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PereMaria.GestorHotel.Views"
    xmlns:controllers="clr-namespace:PereMaria.GestorHotel.Controllers"
    xmlns:components="clr-namespace:PereMaria.GestorHotel.Components"
    mc:Ignorable="d"
    d:DesignHeight="930" d:DesignWidth="1650"
    DataContext="{x:Static controllers:RoomsViewModel.Instance}">

    <Grid Height="926">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
            <!-- Buscar -->
            <StackPanel Orientation="Vertical" Margin="15" Width="200">
                <TextBlock Text="Buscar" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Text-Primary}" Margin="0,20,0,10" />
                <TextBox Style="{StaticResource TextBoxStyle}" />
            </StackPanel>
            
            <!-- Filtrar -->
            <StackPanel Orientation="Vertical" Margin="15" VerticalAlignment="Center">
                <TextBlock Text="Filtrar" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Text-Primary}" Margin="0,20,0,10" />
                <ComboBox Style="{StaticResource DropdownStyle}" Width="200" Margin="0,0,0,10">
                    <ComboBoxItem Content="" />
                    <ComboBoxItem Content="Libre"/>
                    <ComboBoxItem Content="Ocupado"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <!-- Crear -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1" Margin="20">
            <Button Content="Crear" Style="{StaticResource PrimaryButtonStyle}" Margin="10" Height="40" Width="80" Click="Create_Button"/>
        </StackPanel>
        
        <!-- Habitaciones -->
        <Border  BorderBrush="{StaticResource Bg-Purple-Accent}" BorderThickness="2" Grid.Row="1" Margin="20 0 20 0" Grid.ColumnSpan="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"  Background="{StaticResource Bg-Cosmic-Dark}">
                <ItemsControl ItemsSource="{Binding Rooms}" Margin="10 20 10 20" >
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <components:Habitaciones/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </ScrollViewer>
        </Border>
    </Grid>
    
    
</UserControl>